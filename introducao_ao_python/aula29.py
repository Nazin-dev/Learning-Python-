"""
For + Range
range -> range(start, stop, step)
"""
numeros = range(10)
nomef = 0

"""Importante saber que a variavel numero não é o index, mas o proprio valor, pois o for vai percorrendo cada valor dentro da variavel numeros e vai jogando para a variavel numero"""
for numero in numeros:

  print(numero)